# Splice

To be filled in.

You can do some neat stuff:

```console
$ splice --help
Usage: splice [OPTIONS] [COMMAND]

Commands:
  sync    Sync templates in this directory according to the config (the default command)
  config  Show+manipulate configuration
  help    Print this message or the help of the given subcommand(s)

Options:
  -c, --config-path <CONFIG_PATH>  [default: splice.toml]
  -h, --help                       Print help
  -V, --version                    Print version

```
