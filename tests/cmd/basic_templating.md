Given a simple directory, Splice will put create templates:

```console
$ splice sync
[
    Module {
        config: ModuleConfig {
            modules: [],
        },
        args: {},
        prefix: None,
    },
]

```
